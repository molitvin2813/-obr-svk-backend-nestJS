import { NormativeBaseCategory } from './normative-base-category';

describe('NormativeBaseCategory', () => {
  it('should be defined', () => {
    expect(new NormativeBaseCategory()).toBeDefined();
  });
});
